@ Se permiten más retornos que el número de lados izquierdos
PROGRAMA {
    var A := 1UI;
    var E := 1.0F+0;
    var F := 5UI;

    uint, float FUNCION(uint B) {
        var C := A + B;
        var D := 5.0F+0;

        return(C, D);
    }

    A, E, F = FUNCION(4UI->B);
}