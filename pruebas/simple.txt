MAIN {
    print(&Prueba de Operaciones&);

    var A := 20UI;
    var B := 10UI;
    var FOP1 := 2.0F+10;
    print(FOP1);

    @ Prueba de suma
    var C := A + B;
    print(&Suma A + B:&);
    print(C);

    @ Prueba de IF
    if (C > 100UI) {
        print(&C es mayor a 25&);
    } endif;

    @ Prueba de DO WHILE
    do {
        print(B);
        B := B - 1UI;
    } while (B > 5UI);

    var AE := 1UI;
    var BE := 2UI;
    var CE := 3UI;
    print(AE);
    print(BE);
    print(CE);
    AE,BE,CE = 10UI, 20UI, 30UI;
    print(AE);
    print(BE);
    print(CE);

    FOP1 := 2.0F+10;
    var FOP2 := 3.0F+5;
    var FRESPROD := FOP1 * FOP2;
    var FDIV := FOP1 / FOP2;
    print(&DIVISION: &);
    print(FDIV);
    print(&MULTIPLICATION: &);
    print(FRESPROD);

    @var VARUINTMAX := 65535UI;
    @VARUINTMAX := VARUINTMAX + 1221UI;
    @print(VARUINTMAX);

    @var VARFLOAT4 := 3.40282347F+38;
    @VARFLOAT4 := VARFLOAT4 * VARFLOAT4;
    @print(VARFLOAT4);

    @var VARUINTMAX := 65535UI;
    @var VARUINTMIN := 1UI - VARUINTMAX ;
    @print(VARUINTMAX);

    @var JE := 0UI;
    @uint FUNCR(uint JE) {
        @print(&FUNCION&);
        @return (JE);
    @}
    @ AE,BE = FUNCR(11UI->JE), 20UI;
    @ print(AE);
    @ print(BE);
    @ AE = FUNCR(11UI->JE);

    var VARFLOAT4 := 1.40282347F+38;
    var VARUINTMAX := 5UI + VARFLOAT4;
    print(VARUINTMAX);
    print(&Fin del programa&);
}