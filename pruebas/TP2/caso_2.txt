programa_simple {
    uint a, b, c;
    float f1;

    // Asignaciones
    a := 5UI;
    f1 := 1.2 + a;

    // Condicional simple sin else
    if (a == 5UI)
        print(&A es cinco&);
    endif;

    // Bucle simple
    do
        a := a - 1UI;
    while (a > 0UI);

    // Asignación múltiple simple
    a, b = 1UI, 2UI;
}