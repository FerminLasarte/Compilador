@ Copia-Resultado
PROGRAMA {

    uint FUNCION(cr se uint A, cr le uint B, cr le C) {
        A, B, C = 3UI, 5UI, 8UI;
        A := B + C;
        return(A);
    }
    var AUX := 7UI;
    var AUX1 := 8UI;
    var AUX2 := 20UI;
    var PASAJE := FUNCION(AUX->A, AUX1->B, AUX2->C);
}