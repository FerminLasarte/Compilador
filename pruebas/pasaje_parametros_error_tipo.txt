@ Copia-Resultado
PROGRAMA {

    uint FUNCION(cr se uint A, cr le uint B, cr se C) {
        A, B, C = 3UI, 5UI, 8UI;
        A := B + C;
        return(A);
    }
    var AUX := 0UI;
    var PASAJE := FUNCION(18UI->A, AUX->B, AUX->C);
}