@ Se permiten más retornos que el número de lados izquierdos
PROGRAMA {
    var A := 1UI;
    var E := 1.0F+0;
    uint, uint, uint FUNCION(uint B) {
        var C := A + B;
        var D := 5.0F+0;

        return(A, B, C);
    }

    A, B = FUNCION(4UI->B);
}