@ Copia-Resultado
PROGRAMA {

    uint FUNCION(cr se uint A, cr le uint B, uint C) {
        A, B, C = 3UI, 5UI, 8UI;
        A := B + C;

        return(A);
    }

    var PASAJE := FUNCION(0UI->A, 0UI->B, 0UI->C);
}